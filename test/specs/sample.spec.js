import { expect } from 'chai';

import t from '../../src/index';


export default () => {
    describe('This test', () => {
        it('should run', () => {
            //expect(t(2)).to.equal(22);
        });
    });
};
