

export const levels = {
  fatal:   0,
  error:   1,
  warn:    2,
  info:    3,
  verbose: 4,
  debug:   5,
};

export type ELevels = 'fatal' | 'error' | 'warn' | 'info' | 'verbose' | 'debug';


export default {
  levels,
};
